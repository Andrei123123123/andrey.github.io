## Что не так сейчас

Сайт работает, но содержит фактические противоречия и устаревшие данные. Так как сайт информационный, цифры должны быть консистентны во всех секциях, в SEO-разметке и в карте сайта.

## План правок

### 1. Унификация фактов по всему сайту
Зафиксировать единый источник правды:
- **Даты:** 14–20 октября 2026 (везде)
- **Цена спорт-пакета:** €1250
- **Тренировки:** 6 × 1,5 часа = 9 часов на корте
- **Группа:** до 14 человек

Привести к этим значениям:
- `src/pages/Index.tsx` — секция «Программа по дням» (сейчас «6 × 2 ч = 12 часов»), FAQ («2 часа в день»), hero («шесть вечерних тренировок» — ок), время тренировки в дневных карточках (16:00–18:00 → 16:00–17:30)
- `index.html` — JSON-LD `offers.price` (900 → 1250), `subEvent.startDate`, описание
- `public/sitemap.xml` — обновить `lastmod` на сегодня
- Память проекта (`mem://`) — поправить даты на октябрь

### 2. Hero и навигация
- Убрать `<p>` внутри `<button>` если есть; уже используется `<span>` — проверить
- В мобильном меню сейчас 5 соц-иконок в навбаре — на узких экранах перегружено. Оставить в навбаре только Telegram + кнопку «Забронировать», остальное в мобильном меню и футере (там уже есть)

### 3. Секция «Программа по дням»
- Поправить тексты «Вечерняя тренировка 16:00–18:00» → «16:00–17:30 (1,5 ч)»
- День 14 (прилёт) и день 20 (отлёт) — у них нет тренировки, текст уже корректен

### 4. Секция «Команда» (TeamSection)
- Текст ок. Удалить мёртвый файл `src/components/TrainerSection.tsx` — он не импортируется и содержит заглушки `[Имя Фамилия]`, чтобы не попадал случайно в выдачу/бандл

### 5. Секция «Вилла»
- Проверить и убрать дубли в фактах (650 м², 10 спален, до 16 гостей — ок, это вилла, а группа 14 — норм)

### 6. Секция «Стоимость»
- Сейчас две цены: €1250 (спорт) и «от ~160 000 ₽» (тур). В заголовке секции написано «от ~280 000 ₽ из Москвы (≈ €2800)» — это сумма обоих пакетов, но €1250 + €(160000/100≈1600) ≈ €2850. Уточнить математику и привести к одному курсу. Заменить на: «спорт €1250 + тур от ~160 000 ₽ ≈ итого от ~285 000 ₽ из Москвы».
- Текст «Цены действительны до 1 августа 2026» — оставить
- В пакете «Спортивный» добавить уточнение «6 тренировок × 1,5 ч»

### 7. Финальный CTA
- Сейчас `<h2>` с `aria-hidden` и пустым содержимым — это плохо для SEO/доступности. Заменить на нормальный заголовок: «Остались вопросы? Напишите нам».
- Также пустой `<p aria-hidden>` ниже — удалить.

### 8. SEO / `index.html`
- Канонический URL: сейчас `tennis-tenerife.lovable.app`, но есть кастомный домен `tennerife-tennis.com`. Поменять canonical и og:url на основной домен.
- В `<head>` четыре `<meta property="og:image">` подряд — соцсети возьмут только первый. Оставить один основной OG-image (с Google Storage), остальные удалить.
- `og:image` ссылается на временный URL `storage.googleapis.com/...` — оставить, но добавить статичный `/og-image.jpg` как fallback в `og:image:secure_url`
- JSON-LD: цена 900 → 1250, дата `validFrom` → 2026-01-01

### 9. `public/sitemap.xml` и `robots.txt`
- Обновить домен на `https://tennerife-tennis.com/`
- `lastmod` → 2026-05-04

### 10. Память проекта
- Обновить `mem://project/logistics` и связанные: даты — октябрь, тренировки — 1,5 ч, цена €1250

## Файлы, которые будут изменены
- `src/pages/Index.tsx`
- `src/components/TrainerSection.tsx` — удалить
- `index.html`
- `public/sitemap.xml`
- `public/robots.txt`
- `mem://project/logistics`, `mem://commerce/pricing-packages`, `mem://index.md`

## Что НЕ трогаю
- Бэкенд / edge-функцию `send-booking` — работает
- Стили / дизайн-систему — выглядит ок, премиальная подача сохраняется
- Фотогалерея, меню повара, FAQ-структуру

После апрува выполню все правки одним проходом.