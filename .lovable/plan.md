## Изменение H1 в hero-секции

**Файл:** `src/pages/Index.tsx`, строки 188–191

### Было
```
Частная теннисная неделя
на Тенерифе
```

### Станет
```
Теннис, падел и океан
на Тенерифе
```
«на Тенерифе» — золотой курсив (em.text-gold), как сейчас.

### Сопутствующие правки для консистентности

1. **`<title>` в `index.html`** — оставить текущий («Tennerife Tennis Retreat — Тенерифе, 18–24 окт 2026»), без изменений: бренд сохраняется.
2. **og:title / twitter:title** — без изменений (бренд + даты).
3. **JSON-LD Event.name** — без изменений.
4. Все остальные секции — без изменений.

### Что НЕ трогаем
- Подзаголовок «Семь дней. Четырнадцать человек. Одна вилла у океана.» — остаётся.
- Eyebrow «18–24 октября 2026 · Тенерифе · 14 мест» — остаётся.
- CTA-кнопки и стили — остаются.
